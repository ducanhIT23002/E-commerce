# spring.application.name=backend

# # Cấu hình kết nối Aiven MySQL (Đã điền sẵn từ ảnh của bạn)
# spring.datasource.url=jdbc:mysql://mysql-3d8fd727-ducanh-ecommerce-shop.g.aivencloud.com:15966/defaultdb?ssl-mode=REQUIRED
# spring.datasource.username=avnadmin
# spring.datasource.password=PASSWORD
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # Cấu hình Hibernate (Tự động tạo bảng)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.application.name=backend

# Cấu hình để nhận Biến Môi Trường (Quan trọng cho Render)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect